# PR 112 screenshots

Local frontend, real Firebase authentication, Vite proxy straight to the
production Railway backend. No fixtures or seeded data. Desktop 1440×900 and
phone 390×844, full page.

- `list-*` — `/targets`. The MIN · Spot up card carries a saved floor of 15, so
  its Backtest line reads
  `Backtest · season to date · excludes games ≤ 15 min`. The record header
  shows `player-games` with no minutes tile, and no Condition line is repeated
  beneath the heading.
- `workbench-*` — `/targets/10`, the same Target. Two cards: the opponent card
  holding the Qualifier, the Conditions and Why, and a separate `BACKTEST` card
  where the floor sits on the Qualifier's own track, reading `15 min` and
  `100 appearances kept`.
- `workbench-any-desktop` — `/targets/9`, a Target with no floor. The track
  rests on its first stop, reading `any`, and the whole sample is kept.
